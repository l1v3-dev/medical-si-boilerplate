version: "3.3"
services:
  # backend:
  #   ports:
  #     - 5000:5000
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: minimal_back
  #   volumes:
  #     - ./src:/app
  #   networks: ["minimal"]

  mongodb:
    image: mongo:5.0
    ports:
      - 27018:27017
    container_name: medical_si_mongo
    networks: ["medical_si"]

networks:
  medical_si:
