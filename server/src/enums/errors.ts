export enum EServerErrors {
  NOT_FOUND = "not found",
  INVALID_ID = "invalid ID",
  UPDATE_FAILED = "update failed",
}

export enum EAuthenticationError {
  INVALID_CREDENTIALS = "invalid credentials",
}
