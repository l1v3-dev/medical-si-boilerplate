export enum EUserRole {
  ADMIN = "admin",
  MEDECIN = "medecin",
  ACCUEIL = "accueil",
}

export enum EUserGenre {
  MALE = "male",
  FEMALE = "female",
}

export enum EHistoryType {
  USER = "User",
  PRODUCT = "Product",
}

export enum EPrestationType {
  EXTERNE = "externe",
  INTERNE = "interne",
}
