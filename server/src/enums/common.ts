export enum EUserRole {
  ADMIN = "admin",
  CUSTOMER = "customer",
  SELLER = "seller",
}

export enum EUserGenre {
  MALE = "male",
  FEMALE = "female",
}

export enum EHistoryType {
  USER = "User",
  PRODUCT = "Product",
}

export enum EPrestationType {
  EXTERNE = "externe",
  INTERNE = "interne",
}
