export enum EUserRole {
  USER = "user",
  ADMIN = "admin",
  SUPERADMIN = "superadmin",
}

export enum ESubEvents {
  USER_CREATED = "USER_CREATED",
  MESSAGE_CREATED = "MESSAGE_CREATED",
  MESSAGE_UPDATED = "MESSAGE_UPDATED",
}
